#include "io.hpp"
#include <iostream>

namespace biv::io {
    void read_data(std::vector<int>& data) {
        // Реализация чтения
    }

    void print_data(const std::vector<int>& data) {

    }
}