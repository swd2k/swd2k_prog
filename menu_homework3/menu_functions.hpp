#pragma once
#include "menu.hpp"

namespace Swd2k {
    const MenuItem* show_menu(const MenuItem* current);

    const MenuItem* exit(const MenuItem* current);

    const MenuItem* dalniy_vostok(const MenuItem* current);
    const MenuItem* altay(const MenuItem* current);
    const MenuItem* zolotoe_koltso_rossii(const MenuItem* current);
    const MenuItem* geograf_back(const MenuItem* current);

    const MenuItem* vladivostok(const MenuItem* current);
    const MenuItem* sakhalin(const MenuItem* current);
    const MenuItem* khabarovsk(const MenuItem* current);
    const MenuItem* geograf_back2(const MenuItem* current);
}
